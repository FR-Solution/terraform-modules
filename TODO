  # Настроить интеграцию с LOCBOX от YCloud
  #- wget -O /usr/bin/yc           "https://storage.yandexcloud.net/yandexcloud-yc/release/0.96.0/linux/amd64/yc"
  #- chmod +x /usr/bin/yc
  #- yc config profile create my-robot-profile
  #- yc config set cloud-id *
  #- yc config set folder-id *
  #- export IAM_TOKEN=`yc iam create-token`
  #- | # Получаем временный токен для доступа в Vault
  #  curl -X GET -H "Authorization: Bearer $IAM_TOKEN" https://payload.lockbox.api.cloud.yandex.net/lockbox/v1/secrets/e6qprcqkive849prd1kk/payload | jq .entries[0].textValue | sed 's/"//g' > /tmp/key-keeper-tmp.token
