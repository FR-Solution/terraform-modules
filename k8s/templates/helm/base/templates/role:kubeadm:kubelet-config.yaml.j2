apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: kubeadm:kubelet-config-{{ .Values.kubernetes_version_major}}
  namespace: kube-system
rules:
- apiGroups:
  - ""
  resourceNames:
  - kubelet-config-{{ .Values.kubernetes_version_major}}
  resources:
  - configmaps
  verbs:
  - get
